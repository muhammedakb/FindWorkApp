import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    backgroundColor: "#fff",
    width: "100%",
    height: 60,
    borderRadius: 0,
  },
  text: {
    color: "#000",
  },
});
