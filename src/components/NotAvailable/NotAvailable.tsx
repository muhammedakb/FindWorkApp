import React from "react";
import Lottie from "lottie-react-native";

const NotAvailable = () => (
  <Lottie source={require("../../assets/not-available.json")} autoPlay />
);

export default NotAvailable;
